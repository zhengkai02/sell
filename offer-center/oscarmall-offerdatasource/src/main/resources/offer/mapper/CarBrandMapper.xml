<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iwhalecloud.retail.offer.mapper.CarBrandMapper">

    <select id="selectAllList" resultType="com.iwhalecloud.retail.offer.dto.resp.CarBrandResp">
        select * from tbl_car_brand
        where state = 'A'
        order by idx
    </select>
</mapper>