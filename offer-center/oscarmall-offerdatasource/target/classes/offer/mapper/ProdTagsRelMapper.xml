<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iwhalecloud.retail.offer.mapper.ProdTagsRelMapper">

    <select id="listTagIdByProdGoodsId" resultType="java.lang.String">
        select a.tag_id
          from tbl_prod_tags_rel a
           where a.state = 'A'
           and a.goods_id = #{goodsId}
    </select>

    <update id="deleteByGoodsId">
        update tbl_prod_tags_rel
         set state = 'X', modify_by = #{modifyBy}, modify_time = now()
         where goods_id = #{goodsId}
    </update>

    <select id="listProdGoodsIdByTagId" resultType="java.lang.String">
        select a.goods_id
          from tbl_prod_tags_rel a
           where a.state = 'A'
           and a.tag_id = #{tagId}
    </select>

</mapper>